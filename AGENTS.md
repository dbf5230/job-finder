[RULES-OK]

# Agent Instructions

## 제품 요구사항

- 충남대학교 컴퓨터인공지능학부 1학년이 소프트웨어·서비스 개발 중심의 인턴·신입 공고를 빠르게 탐색하는 단일 화면을 만든다.
- 지원 가능한 공고, 제외된 공고, 관심 공고를 분리하고 공고 상세 확인과 관심 공고 저장을 제공한다.
- 공고에 명시된 데이터만으로 지원 가능 여부와 정렬 순서를 판정한다.

## 지원 가능 판정과 정렬

- 소프트웨어·서비스 개발이 주된 업무이고 근무지가 수도권·대전·세종·충남인 공고만 지원 후보로 분류한다.
- 졸업자·졸업예정자만 지원 가능하거나, 경력이 필수이거나, 1학년이 지원할 수 없는 공고는 제외 이유와 함께 제외한다.
- 우선순위는 게임 관련 공고, 수도권·대전·세종·충남, 가까운 마감일 순으로 적용한다.
- 마감일까지 7일 이내인 공고도 게임 관련 여부와 지역이 같은 공고 안에서 마감일순으로 표시한다.
- 인턴·신입, 회사 유형, 근무 방식은 우선순위에 영향을 주지 않는다.
- 관심 공고는 저장한 날짜가 최신인 순서로 정렬한다.

## 화면 동작

- 상세 내용을 열기 전에는 너비가 노트북 화면의 약 3분의 1인 슬라이드형 공고 카드를 중앙에 표시한다.
- 공고를 선택하면 현재 목록과 슬라이드 위치를 유지한 채 카드 영역을 왼쪽으로 옮기고 상세 내용을 오른쪽에 표시한다.
- `지원 가능`, `제외된 공고`, `관심 공고` 탭에는 해당 공고만 표시한다.
- `우선순위순`, `인턴`, `신입` 필터는 지원 가능한 공고에 지정된 정렬 또는 모집 형태를 적용한다.
- 카드에는 기관명, 공고 제목, 지역, 마감일, 작동하는 원문 링크를 표시한다.
- 상세에는 주요 업무, 개발 중심 여부, 근무 지역, 지원 자격, 인턴·신입 구분, 근무 방식, 마감일과 접수 상태, 지원 가능 여부 또는 제외 이유를 표시한다.
- 상세에서 관심 공고 저장 상태를 전환할 수 있게 한다.
- 흰색 `#FFFFFF`, 검정색 `#000000`, 보라색 `#E39EFF`와 나눔고딕을 사용해 모던하고 깔끔하게 표현한다.

## 저장소 구조

| 영역 | 경로 |
|---|---|
| 화면과 예시 공고 데이터 | `app/page.tsx` |
| 전역 스타일과 반응형 레이아웃 | `app/globals.css` |
| 메타데이터와 루트 레이아웃 | `app/layout.tsx` |
| 렌더링 검증 | `tests/rendered-html.test.mjs` |
| Sites 설정 | `.openai/hosting.json` |

## 명령

| 작업 | 명령 |
|---|---|
| 의존성 설치 | `npm install` |
| 로컬 실행 | `npm run dev` |
| 린트 | `npm run lint` |
| 프로덕션 빌드 | `npm run build` |
| 렌더링 테스트 | `npm test` |
| DB 스키마 생성 | `npm run db:generate` |

## 작업 규칙

- npm과 `package-lock.json`을 사용하고 Node.js `>=22.13.0`을 유지한다.
- UI 변경은 `app/`에서 하고 `.next/`, `.vinext/`, `dist/` 생성물은 직접 수정하지 않는다.
- 현재 `tests/rendered-html.test.mjs`는 스타터 로딩 화면을 검증하므로 실제 화면 변경 시 함께 갱신한다.
- DB가 필요할 때만 `db/schema.ts`를 수정하고 이후 `npm run db:generate`를 실행한다.
- 인증 또는 Sites 바인딩을 변경할 때는 `README.md`와 `.openai/hosting.json`을 먼저 확인한다.
- 변경한 파일에 린트를 실행하고, 화면 동작 변경 후에는 빌드와 렌더링 테스트를 실행한다.
